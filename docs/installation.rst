Installation
============

Using conda
-----------

Create the project environment with:

.. code-block:: bash

   conda env create -f environment.yml
   conda activate neuromass

Using pip
---------

For editable development installation:

.. code-block:: bash

   pip install -e .[dev,docs,test]

